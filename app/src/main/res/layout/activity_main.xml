<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:orientation="vertical"
    android:background="#80B6D1"
    android:padding="16dp">

    <ImageView
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:id="@+id/imgLogo"
        android:src="@drawable/file"
        android:layout_centerInParent="true"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/tvDeskripsi"
        android:text="Berikut adalah halaman portal sekolah, semua informasi terkait sekolah bisa di akses"
        android:layout_below="@id/imgLogo"
        android:gravity="center"
        android:layout_marginTop="15dp"/>

    <Button
        android:layout_width="270dp"
        android:layout_height="45dp"
        android:text="Log in"
        android:id="@+id/btnLogin"
        android:layout_below="@id/tvDeskripsi"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="15dp"
        android:backgroundTint="#15346A"/>

    <Button
        android:layout_width="270dp"
        android:layout_height="45dp"
        android:text="Sign Up"
        android:id="@+id/btnRegister"
        android:layout_below="@id/btnLogin"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="15dp"
        android:textColor="@color/white"
        android:backgroundTint="#22A5F0"/>

</RelativeLayout>